import { createAction } from '@ngrx/store';

export const cartDetailsOpened = createAction('[Cart Details Page] Opened');

export const purchaseSuccess = createAction(
  '[Cart Details Page] Purchase successful'
);
